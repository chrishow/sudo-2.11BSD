/*
 * string.h wrapper for 2.11BSD
 * The system's string.h is broken, so we redirect to strings.h
 */

#ifndef _SUDO_STRING_H_
#define _SUDO_STRING_H_

#include <strings.h>

#endif /* _SUDO_STRING_H_ */
